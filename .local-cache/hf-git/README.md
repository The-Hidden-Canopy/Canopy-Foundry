# Optional local Hugging Face/Git cache

This directory is a local-only cache root. The repository ignores everything
under it except this pointer file. Use
[`docs/hf-git-cache.md`](../../docs/hf-git-cache.md) and
`scripts/enable_hf_git_cache.ps1` to configure a shell without putting tokens
or downloaded artifacts in Git.
